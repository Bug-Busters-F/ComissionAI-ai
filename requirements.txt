# --- API ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- LLM providers (instale apenas o do seu provedor escolhido) ---
# pip install google-genai      # LLM_PROVIDER=gemini
# openai>=1.30.0                # LLM_PROVIDER=openai
# anthropic>=0.28.0             # LLM_PROVIDER=anthropic
# groq>=0.9.0                   # LLM_PROVIDER=groq

# --- HTTP client (chamadas Spring → Python → Spring, Sprint 2) ---
httpx>=0.27.0
